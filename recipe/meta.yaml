{% set version = "1.6.2" %}
{% set name = "linbox" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://github.com/linbox-team/linbox/releases/download/v{{ version }}/{{ name }}-{{ version }}.tar.gz
  sha256: b00d174f5f875f20f86730882e77cbf30fa392a2241fd7aa2f467b2c975d020c
  patches:
    # An upstream patch that Sage uses for 8.2: https://github.com/sagemath/sage/blob/8.2.rc4/build/pkgs/linbox/patches/linbox_charpoly_fullCRA.patch
    - patches/9e6e5dd-f4b483c.patch

build:
  number: 0
  skip: true  # [win]

requirements:
  build:
    - libtool
    - automake
    - pkg-config
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - libblas
    - libcblas
    - gmp
    - mpfr
    - ntl
    - libflint
    - givaro
    - iml
    - fflas-ffpack
    - m4ri
    - m4rie
  run:
    - gmp
    - mpfr
    - ntl
    - libflint
    - {{ pin_compatible("givaro", max_pin="x") }}
    - iml
    - {{ pin_compatible("fflas-ffpack", max_pin="x.x") }}
    - m4ri
    - m4rie

test:
  commands:
    - test -f ${PREFIX}/include/linbox/integer.h
    - test -f ${PREFIX}/lib/lib{{ name }}.a
    - test -f ${PREFIX}/lib/lib{{ name }}.dylib     # [osx]
    - test -f ${PREFIX}/lib/lib{{ name }}.so        # [linux]

about:
  home: http://linalg.org/
  license: LGPLv2.1
  license_file: COPYING.LESSER
  summary: C++ library for exact, high-performance linear algebra
  dev_url: https://github.com/linbox-team/linbox

extra:
  recipe-maintainers:
    - isuruf
    - saraedum
